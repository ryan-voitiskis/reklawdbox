---
title: MCP Tools
description: All MCP tools exposed by the reklawdbox server, grouped by function.
sidebar:
  order: 0
---

import { Card, CardGrid, Aside } from '@astrojs/starlight/components';

reklawdbox exposes MCP tools for reading your library, enriching metadata, analyzing audio, building sets, discovering pools, and exporting changes. All tools accept and return JSON.

## Tool pipeline

A typical workflow flows left to right through these tool groups:

```
Library & Data ──> Enrichment & Analysis ──> Classification & Staging ──> Export
       |                                              |
       └──────── Mixing & Sequencing ─────────────────┘
                         |
               Files & System (direct file ops, audit, cache management)
```

## Tool groups

<CardGrid>
  <Card title="Library & Data" icon="magnifier">
    Search tracks, read playlists, resolve cached enrichment and analysis data.
    [11 tools](/mcp-tools/library-data/)
  </Card>
  <Card title="Enrichment & Analysis" icon="rocket">
    Look up metadata on Discogs and Beatport. Analyze audio for BPM, key, and energy descriptors.
    [8 tools](/mcp-tools/enrichment-analysis/)
  </Card>
  <Card title="Classification & Staging" icon="pencil">
    Genre classification, auditing, metadata backfill, staging, and XML export.
    [11 tools](/mcp-tools/classification-staging/)
  </Card>
  <Card title="Mixing & Sequencing" icon="bars-ascending">
    Score transitions, build set sequences, and discover compatible track pools.
    [10 tools](/mcp-tools/mixing/)
  </Card>
  <Card title="Files & System" icon="setting">
    Read/write native file tags, manage cover art, audit collections, and clear caches.
    [11 tools](/mcp-tools/files-system/)
  </Card>
</CardGrid>

## Shared search filters

Many tools share a common set of **search filter parameters**. Used by `search_tracks`, `get_play_stats`, `enrich_tracks`, `analyze_audio_batch`, `resolve_tracks_data`, and `cache_coverage`.

| Parameter | Type | Description |
|-----------|------|-------------|
| `query` | string | Free-text search matching title or artist |
| `artist` | string | Filter by artist (partial match) |
| `genre` | string | Filter by genre (partial match) |
| `rating_min` | integer | Minimum star rating (1-5) |
| `bpm_min` | number | Minimum BPM |
| `bpm_max` | number | Maximum BPM |
| `key` | string | Filter by musical key (e.g., `"Am"`, `"Cm"`) |
| `has_genre` | boolean | Filter by whether track has a genre set |
| `has_label` | boolean | Filter by whether track has a label set |
| `year_zero` | boolean | Filter to tracks with year = 0 (unset) |
| `label` | string | Filter by label (partial match) |
| `path` | string | Filter by file path/folder (substring match) |
| `path_prefix` | string | Filter to tracks whose file path starts with this prefix |
| `added_after` | string | ISO date — only tracks added on or after |
| `added_before` | string | ISO date — only tracks added on or before |

<Aside type="tip">
  Filters are combinable: `genre: "Techno"` + `bpm_min: 130` + `has_genre: true` scopes to up-tempo techno with genres assigned.
</Aside>
