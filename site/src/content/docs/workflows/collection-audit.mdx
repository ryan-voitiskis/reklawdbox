---
title: Collection Audit
description: Detect and fix naming, tagging, and convention violations in your music files.
sidebar:
  order: 1
---

import { Aside } from '@astrojs/starlight/components';
import CollectionAuditSOP from '../../../partials/sops/collection-audit.mdx'

Fix naming, tagging, and convention violations so tracks import into Rekordbox correctly. Genre is handled separately by [genre classification](/workflows/genre-classification/).

**Paste into your agent to start:**

```
Audit my collection for naming and tagging issues.
```

## WAV tagging

<Aside type="caution">
  Rekordbox reads **only RIFF INFO (tag 3)** from WAV files, completely ignoring ID3v2 tags. If your WAV files only have ID3v2 tags (common with files from Bandcamp, Beatport, etc.), Rekordbox will show them as untagged.
</Aside>

Both ID3v2 and RIFF INFO must be written for full compatibility across players. The `WAV_TAG3_MISSING` and `WAV_TAG_DRIFT` checks catch these problems, and `write_file_tags` handles writing both tag formats automatically.

## Imported file protection

<Aside type="caution">
  Imported files are **never** renamed — Rekordbox stores absolute paths and can't update them via XML. To rename an imported file: delete from Rekordbox, rename, re-import.
</Aside>

## File naming conventions

### Album tracks

```
NN Artist Name - Track Title.ext
```

Examples: `01 Actress - Maze.flac`, `12 Four Tet - Baby.wav`

### Loose tracks (singles, downloads)

```
Artist Name - Track Title.ext
```

Examples: `Burial - Archangel.flac`, `Joy Orbison - Hyph Mngo.wav`

### Album directories

```
Album Name (Year)/
```

Examples: `Untrue (2007)/`, `New Energy (2017)/`

## Step-by-step

<CollectionAuditSOP />

## Next steps

- [Genre Classification](/workflows/genre-classification/) — classify genres for ungenred tracks
- [Set Building](/workflows/set-building/) — build transition-scored set sequences once your metadata is clean
