---
title: Genre Classification
description: Classify genres using Discogs, Beatport, and audio analysis evidence.
sidebar:
  order: 2
---

import { Aside } from '@astrojs/starlight/components';
import WorkflowContract from '../../../components/WorkflowContract.astro'
import GenreClassificationSOP from '../../../partials/sops/genre-classification.mdx'

<WorkflowContract id="genre-classification" />

Paste into your agent:

```
Classify genres for my ungenred tracks.
```

## How it works

The agent runs a genre decision tree against all your cached evidence — Discogs styles, Beatport genres, label associations, BPM, and audio features. Results come back grouped by confidence:

- **High** — multiple sources agree and BPM is plausible
- **Medium** — partial agreement or single strong source
- **Low** — weak or ambiguous evidence
- **Insufficient** — conflicting data, needs a manual call

High and medium confidence results can usually be approved in bulk. Low and insufficient need a closer look.

## Prerequisites

Run scoped `cache_coverage` for ungenred tracks. Fill missing core Discogs and
Beatport searches and Stratum analysis where practical; a cached no-match counts
as searched. Essentia improves the audio evidence but is not a hard gate. You
can proceed with incomplete evidence: affected tracks should fall to low or
insufficient confidence and receive individual review.

## Genre taxonomy

The built-in taxonomy has ~50 genres, tuned for DJ mixing — specific where it matters (Deep House and Afro House play very differently) without being overly granular. It's a starting point, not a closed list. You can add genres during classification when the evidence supports it.

## Step-by-step

<GenreClassificationSOP />

## Next steps

- [Genre Audit](/workflows/genre-audit/) — verify existing genre tags against evidence
- [Set Building](/workflows/set-building/) — build sets using your classified genres
